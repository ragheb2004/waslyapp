{
  "version": 2,
  "installCommand": "cd food-delivery && composer install --no-dev --prefer-dist --optimize-autoloader && npm install",
  "buildCommand": "cd food-delivery && npm run build && cd .. && rm -rf dist && mkdir -p dist && cp -r food-delivery/public/* dist/",
  "outputDirectory": "dist",
  "builds": [
    {
      "src": "api/index.php",
      "use": "@vercel/php"
    },
    {
      "src": "food-delivery/public/build/**",
      "use": "@vercel/static"
    },
    {
      "src": "food-delivery/public/**",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/build/(.*)",
      "dest": "/food-delivery/public/build/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/api/index.php"
    }
  ]
}
